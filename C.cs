#nullable enable
using System;
using System.Collections.Generic;
using System.Linq;
using static System.Console;

namespace AtCoder
{
    internal static class C
    {
        // static void Main()
        // {
        //     var x = ReadLine()!;
        //     var n = int.Parse(ReadLine()!);
        //     var names = new string[n];
        //
        //     for (var i = 0; i < n; ++i)
        //     {
        //         names[i] = ReadLine()!;
        //     }
        //
        //     var newNames = new List<(string, string)>();
        //
        //     for (var i = 0; i < n; ++i)
        //     {
        //         var name = names[i];
        //         var chars = new char[name.Length];
        //
        //         for (var j = 0; j < name.Length; ++j)
        //         {
        //             var index = name[j] - 97;
        //             chars[j] = x[index];
        //         }
        //
        //         newNames.Add((new string(chars), name));
        //     }
        //
        //     newNames.Sort((a, b) => string.CompareOrdinal(a.Item1, b.Item1));
        //
        //     foreach (var (newName, oldName) in newNames)
        //     {
        //         WriteLine(oldName);
        //     }
        // }
    }
}
