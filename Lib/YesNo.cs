#nullable enable
using System;

namespace AtCoder.Lib
{
    static class YesNo
    {
        internal static void Write(bool isYes)
        {
            Console.WriteLine(isYes ? "Yes" : "No");
        }
    }
}
